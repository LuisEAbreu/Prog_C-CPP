#include <stdio.h>

/*
Implemente um programa em C que percorre um intervalo [a,b],
com passo dx = 0.01, e determina o valor máximo e mínimo positivos da função f(x).
Use continue para ignorar valores negativos ou nulos da função. A função é f(x)=x²-4x+3.
O intervalo é fornecido como entrada e deve ser válido, ou seja, a e b reais sendo b>a.
Solicitar uma nova entrada caso isso não seja atendido.
*/

int main(){
    float a,b,min,max;

    

    return 0;
}